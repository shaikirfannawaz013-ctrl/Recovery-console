# Base path of the Spring Boot REST API
VITE_API_BASE_URL=/api
# STOMP WebSocket endpoint that relays Kafka transaction events
VITE_WS_URL=ws://localhost:5173/ws
# true = run fully on in-browser mock data (no backend needed)
VITE_USE_MOCK=true
